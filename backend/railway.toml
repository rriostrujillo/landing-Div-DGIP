[build]
builder = "docker"
builder = "nixpacks"
dockerfile = "Dockerfile"

[deploy]
numReplicas = 1
restartThresholdType = "error"
restartThreshold = 5

[[services]]
name = "dgip-backend"
region = "eu-west"
healthcheckPath = "/"
healthcheckTimeout = 30
restart = true